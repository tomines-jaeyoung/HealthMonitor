<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthMonitor">

        <!-- 메인 대시보드 (눈동자) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.HealthMonitor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- 페이스 스캐닝 -->
        <activity android:name=".FaceScanActivity" />

        <!-- 센서 확인 -->
        <activity android:name=".SensorCheckActivity" />

        <!-- 실시간 스캔 (Chronometer + ToggleButton) -->
        <activity android:name=".ScanningActivity" />

        <!-- 혈류 측정 -->
        <activity android:name=".BloodFlowActivity" />

        <!-- 파형 데이터 -->
        <activity android:name=".WaveformActivity" />

        <!-- 달력/히스토리 -->
        <activity android:name=".HistoryActivity" />

        <!-- 사용자 정보 입력 -->
        <activity android:name=".UserInfoActivity" />

        <!-- 긴급 연락처 -->
        <activity android:name=".EmergencyContactActivity" />

        <!-- 측정 항목 선택 -->
        <activity android:name=".MeasurementSelectActivity" />
        <activity android:name=".HeartRateActivity" />
        <activity android:name=".SpO2Activity" />
        <activity android:name=".TemperatureActivity" />
        <activity android:name=".HeartRateResultActivity" />
        <activity android:name=".SpO2ResultActivity" />
        <activity android:name=".TemperatureResultActivity" />
        <activity android:name=".FullMeasureActivity" />
        <activity android:name=".IndividualReportActivity" />

    </application>

</manifest>
